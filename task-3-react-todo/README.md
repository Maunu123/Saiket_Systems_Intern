# Task 3: React Task Manager

Run `npm install`, then `npm run dev`. The app demonstrates component rendering, state, controlled inputs, list rendering, filtering, editing, deleting, and completion toggles.
